import NFT from './NFT'
const NFTList = ({nfts}) => {
    // console.log(nfts)
    return(
        <div className='nft_wrapper'>
            {/* {  
                nfts.map((nft)=>{
                    return (<NFT key={nft.id} nft={nft}/>)
                })
            } */}
            
        </div>
    );
}
export default NFTList;